<%= render 'users/list', user: @user ,book: @book %>
<h1>Users</h1>
<% @users.each do |user| %>
  <table>
    <thead>
      <tr>
        <th>image</th>
        <th>name</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <rd></rd>
        <rd><%= user.name%></rd>
        <rd><%= link_to"Show" ,user_path(@user.id)%></rd>
      </tr>
    </tbody>
  </table>
<% end %>
  
  
  