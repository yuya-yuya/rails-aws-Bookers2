<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/list', user: @user ,book: @book %>
    </div>
    <div class="col-md-9">
      <h2>Books</h2>
      <% @books.each do |book| %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <%= link_to"prof"%>
              
            </td>
            <td><%= link_to book.title, book_path(current_user)%></td>
            <td><%= book.body %></td>
          </tr>
        </tbody>
      </table>
      <% end %>
      </div>
    </div>
  </div>
</div>